apiVersion: v1
kind: Secret
metadata:
  name: athena-secrets
  namespace: athena
type: Opaque
stringData:
  # 生产环境请用 kubectl create secret / 外部 Secret 管理器注入，勿提交真实值
  ATHENA_API_KEYS: "changeme-key:tenant-a"
  OPENAI_API_KEY: ""
  DEEPSEEK_API_KEY: ""
